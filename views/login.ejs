<%- include(pathviews + '/partials/header');%>

    <div class="container">
      <div class="text-center">
        <h1></h1>
        <br />
        <img id="trust-wallet-connect" src="">
        <!--
          <a id="eth" onclick="ethClicked()">Login with Ethereum account</a>
        -->
        <p id="nonce" hidden><%=nonce%></p>
        <button
          type="button"
          class="btn btn-large"
          id="login-btn"
          onclick="login()"
        >
          Login with web3Modal
        </button>
      </div>
    </div>


<%- include(pathviews + '/partials/footer');%>
